<section class="price-section" aria-labelledby="price-title">

  <div class="price-card" lang="en">

    <h1 id="price-title">
      CLASS PASSES
    </h1>

    <section
      class="price-category"
      aria-labelledby="price-yoga-title"
    >
      <h2 id="price-yoga-title">
        YOGA
      </h2>

      <dl>
        <div>
          <dt>DROP-IN</dt>
          <dd>₩35,000</dd>
        </div>

        <div>
          <dt>8-CLASS PASS</dt>
          <dd>₩240,000</dd>
        </div>

        <div>
          <dt>12-CLASS PASS</dt>
          <dd>₩336,000</dd>
        </div>

        <div>
          <dt>24-CLASS PASS</dt>
          <dd>₩600,000</dd>
        </div>

        <div>
          <dt>36-CLASS PASS</dt>
          <dd>₩864,000</dd>
        </div>
      </dl>
    </section>


    <section
      class="price-category"
      aria-labelledby="price-sound-title"
    >
      <h2 id="price-sound-title">
        SOUND BATH
      </h2>

      <dl>
        <div>
          <dt>SINGLE SESSION</dt>
          <dd>₩30,000</dd>
        </div>
      </dl>
    </section>

  </div>


  <section
    class="price-guide"
    aria-labelledby="price-guide-title"
  >
    <h2 id="price-guide-title">
      이용 안내
    </h2>

    <ul>
      <li>
        <span lang="en">Drop-in</span>:
        구매일로부터 14일 이내 사용
      </li>

      <li>
        <span lang="en">8-Class Pass</span>:
        첫 수업일부터 2개월
      </li>

      <li>
        <span lang="en">12-Class Pass</span>:
        첫 수업일부터 3개월
      </li>

      <li>
        <span lang="en">24-Class Pass</span>:
        첫 수업일부터 4개월
      </li>

      <li>
        <span lang="en">36-Class Pass</span>:
        첫 수업일부터 5개월, 1회 최대 4주 홀딩 가능
      </li>

      <li>
        클래스 패스는 구매 후 30일 이내 시작
      </li>
    </ul>
  </section>

</section>


<style>
  .price-section {
    width: min(
      calc(100% - (var(--page-gutter) * 2)),
      500px
    );

    margin-inline: auto;

    padding:
      clamp(150px, 18vh, 220px)
      0
      clamp(100px, 12vh, 160px);

    color: var(--color-ink);
  }


  /* ========================================
     PRICE
  ======================================== */

  .price-card {
    width: 100%;

    font-size: clamp(13px, 2.4vw, 16px);
    line-height: 1.5;
    letter-spacing: .04em;

    user-select: text;
  }

  .price-card h1 {
    margin: 0;

    padding-bottom: 24px;

    font-family: var(--font-mono);
    font-size: clamp(22px, 4vw, 28px);
    font-weight: 600;
    line-height: 1;
    letter-spacing: .04em;
  }


  /* ========================================
     CATEGORIES
  ======================================== */

  .price-category {
    border-top: 1px solid currentColor;
    padding-top: 24px;
  }

  .price-category + .price-category {
    margin-top: 32px;
  }

  .price-category h2 {
    margin: 0 0 20px;

    font-family: var(--font-mono);
    font-size: clamp(17px, 3vw, 20px);
    font-weight: 600;
    line-height: 1.3;
    letter-spacing: .04em;
  }

  .price-card dl {
    margin: 0;

    font-family: var(--font-mono);
    font-weight: 500;
    letter-spacing: .04em;
  }

  .price-card dl > div {
    display: grid;

    grid-template-columns:
      minmax(0, 1fr)
      max-content;

    gap: 16px;

    padding-block: 8px;
  }

  .price-card dt {
    overflow-wrap: anywhere;
  }

  .price-card dd {
    margin: 0;
    text-align: right;
  }


  /* ========================================
     GUIDE
  ======================================== */

  .price-guide {
    width: 100%;

    margin-top: 28px;

    color: var(--color-muted);

    font-size: 12px;
    line-height: 1.8;
    letter-spacing: -.02em;

    word-break: keep-all;
    overflow-wrap: anywhere;
  }

  .price-guide h2 {
    margin: 0 0 8px;

    color: var(--color-ink);

    font-size: 13px;
    font-weight: 400;
  }

  .price-guide ul {
    margin: 0;
    padding-left: 16px;
  }


  /* ========================================
     MOBILE
  ======================================== */

  @media (max-width: 700px) {
    .price-section {
      width:
        calc(
          100% -
          (var(--page-gutter) * 2)
        );

      padding:
        130px
        0
        90px;
    }

    .price-card h1 {
      font-size: 22px;
    }

    .price-category h2 {
      font-size: 17px;
    }
  }
</style>